{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "echo \"build api\"",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo \"linting api\""
  },
  "dependencies": {
    "fastify": "latest",
    "@ledgerlens/shared": "workspace:*",
    "@ledgerlens/ingestion": "workspace:*",
    "@ledgerlens/reconciliation-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "latest",
    "@types/node": "latest"
  }
}
